#include <stdio.h>

int main() {
    int age = 42;
    float number = 22.7;
    char sign='#';
    double value = 50.33;
        printf("age is %d \n", age);
        printf("number is %f \n", number);
        printf("sign is %c \n", sign);
        printf("double is %lf \n", value);
    return 0;
    }